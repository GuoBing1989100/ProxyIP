name: Rust CF Scan

on:
  workflow_dispatch: # 可以手动运行
  #schedule:
  #  - cron: '0 */6 * * *'  # 每 6 小时运行一次

jobs:
  update-proxies:
    runs-on: ubuntu-latest
    name: 🔎 Scan Proxy Rust Binary

    steps:
    - name: 📂 Checkout Repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        token: ${{ secrets.GITHUB_TOKEN }}   # 用默认令牌即可

    - name: 🛠️ Install Rust Toolchain
      uses: dtolnay/rust-toolchain@stable

    - name: 📦 Build Rust Project (Release)
      run: cargo build --release

    - name: 🧪 Give Execute Permission to Binary
      run: chmod +x ./target/release/cekproxy

    - name: 🚀 Run Rust Binary
      run: ./target/release/cekproxy

    - name: 📤 Commit and Push Changes
      if: ${{ success() }}
      run: |
        git config --global user.name "Github Actions"
        git config --global user.email "actions@github.com"
        git add Data/alive.txt Data/Country-ALIVE.txt
        git commit -m " 🔁 Alive & Country-ALIVE"
        git push https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/<你的用户名>/<你的仓库名>.git main --force
      shell: bash
